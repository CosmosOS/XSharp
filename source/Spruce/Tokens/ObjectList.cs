using System;
using System.Collections.Generic;
using System.Text;

namespace Spruce.Tokens {
    public class ObjectList<T> : Token {
        // TODO uses a dictionary<string, T> and returns transformed objects from it (Just not needed yet)
        // Could also be used to do the upper, lower etc, ie the new token is ObjectList and is ancestor and this becomes string list
    }
}
